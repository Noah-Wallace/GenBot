services:
  - type: web
    name: genai-healthcare
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"   # ✅ Proper production server
    envVars:
      - key: GROQ_API_KEY
        value: your-groq-api-key
